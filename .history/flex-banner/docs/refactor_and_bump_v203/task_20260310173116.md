# タスク: ディレクトリ構造の見直しとバージョンアップ

## 概要
リポジトリ内の不要な階層（`flex-banner/flex-banner/`）を解消し、ルートにファイルを配置する。また、更新テストのためにバージョンを上げる。

## 目標
- [x] バージョンを `2.0.3` に更新
- [x] ディレクトリ構造をフラット化（`flex-banner/` フォルダの中身をルートへ移動）
- [x] GitHub へのプッシュと公開
- [x] 更新機能の再確認
