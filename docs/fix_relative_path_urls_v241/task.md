# タスク: 相対パス入力時のURL補完の修正 (v2.4.1)

## 概要
バナーやボタンのリンク先としてスラッグ（相対パス）のみを入力した際、WordPressがサブディレクトリ階層（例: `/dev/001_development/`）にインストールされている場合でも正しいルートURLを参照してリンクを生成できるよう修正します。

## ステップ
1. [x] `includes/class-fb-shortcode.php` の修正
   - URLを正規化するためのヘルパーメソッド `format_banner_url` を追加。
   - `esc_url` が自動適用される前に、`home_url('/')` の階層を考慮してスラッグとドメインを正しい形で結合する処理を追加。
2. [x] バージョンアップ
   - バージョンを 2.4.1 に更新。
3. [x] Git へのプッシュとタグ `v2.4.1` の発行。
