# 実装計画: プライベートリポジトリ認証設定 (v2.0.6)

## 現状の課題
- リポジトリがプライベート設定のため、認証なしでは GitHub API から 404 エラーが返り、更新チェックができない。

## 手順
1. **コードの修正 (`flex-banner.php`)**
   - `Version` を `2.0.6` に更新。
   - `setAuthentication('github_pat_...')` を追加し、提供されたトークンで認証を行う。
2. **Git への反映**
   - 変更をコミットし、プッシュ。
3. **確認**
   - GitHub に正常にプッシュされたことを確認。
